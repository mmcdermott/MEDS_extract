# Single MESSY file for the example ETL: carries BOTH the ``sources:`` block that
# ``meds-extract-download`` reads AND the event-conversion config that the MEDS_extract
# pipeline reads. ``MessyConfig.parse`` treats ``sources`` as a reserved top-level key
# (alongside ``_defaults``), so ``convert_to_MEDS_events`` / ``shard_events`` / etc. skip
# it when iterating table definitions.
#
# ``${oc.env:EXAMPLE_RAW_DATA}`` is resolved at CLI-invocation time; the README
# walkthrough + the integration test both set it before running.

sources:
  # Reserved key: the raw-data release version per bucket (never itself a bucket).
  # ``meds-extract-run`` stamps the selected bucket's entry into the output's
  # ``etl_metadata.dataset_version``, and — being an ordinary document node — it is
  # interpolatable into source entries, as the ``common`` bucket's URL shows.
  dataset_version:
    dataset: "0.1"
    common: "2.2"
  dataset:
    - type: fsspec
      root: ${oc.env:EXAMPLE_RAW_DATA}
  common:
    - type: physionet
      base_url: https://physionet.org/files/mimic-iv-demo/${sources.dataset_version.common}

# Reserved ``etl:`` block consumed only by ``meds-extract-run`` (issue #170): with it,
# this one file describes the ENTIRE ETL — where the data lives (``sources:``), what to
# extract (the event tables below), and how to run it. The runner always runs the
# canonical 8-stage extraction pipeline; the flat keys below are the curated per-stage
# options (real stage-parameter names), carrying the same values as ``pipeline.yaml``
# so both invocation styles produce bit-identical outputs (``seed`` is omitted: the
# MEDS-transforms default of 1 matches ``pipeline.yaml``'s explicit ``seed: 1``).
# ``dataset_name`` is needed here because the example spec is resolved by path —
# registered specs inherit their entry-point name and can omit it.
etl:
  dataset_name: MEDS_extract_example
  row_chunksize: 2
  n_subjects_per_shard: 10
  split_fracs:
    train: 0.5
    tuning: 0.25
    held_out: 0.25
  do_dedup_text_and_numeric: false

_defaults:
  subject_id: $patient_id

patients:
  eye_color:
    code: 'f"EYE_COLOR//{$eye_color}"'
    time: null
  hair_color:
    code: 'f"HAIR_COLOR//{$hair_color}"'
    time: null
  dob:
    code: MEDS_BIRTH
    time: '$dob::"%Y-%m-%dT%H:%M:%S"'
  dod:
    code: MEDS_DEATH
    time: '$dod::"%Y-%m-%dT%H:%M:%S"'

labs_vitals:
  _table:
    join:
      stays:
        key: stay_id
        cols: [patient_id]
  lab:
    code: $test_name
    time: '$timestamp::"%Y-%m-%dT%H:%M:%S"'
    numeric_value: $result
    _metadata:
      lab_descriptions:
        test_name: $test_name # join key: name matches the code's component
        description: $description

medications:
  med:
    code: 'f"{$medication_name}//{$dose}"'
    time: '$timestamp::"%Y-%m-%dT%H:%M:%S"'
    _metadata:
      medication_classes:
        # Partial match: only `medication_name` (of the code's two components) is
        # produced, so the class broadcasts to every dose-variant of the medication.
        medication_name: $medication_name
        description: $drug_class

diagnoses:
  dx:
    code: $diagnosis_code
    time: '$timestamp::"%Y-%m-%dT%H:%M:%S"'
